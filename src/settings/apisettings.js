// Clase que tiene los parametros de la API
export class ApiSettings{

    // Puerto donde se situa la API
    static port = 9000;

    // Ruta por defecto de la API
    static serverurl = `http://localhost:${this.port}`;
    
}